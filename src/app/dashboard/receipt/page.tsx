import OverviewReceiptView from 'src/sections/overview/receipt/view/overview-receipt-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Thông báo',
};

export default function NotificationPage() {
  return <OverviewReceiptView />;
}
